name: Deploy

on:
  push:
    branches:
      -  AnnikaCodes-patch-1

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy Server
        # ... your existing deploy steps ...

      - name: Trigger Client in Coolify
        run: |
          curl -X GET "${{ secrets.COOLIFY_WEBHOOK_URL }}"
