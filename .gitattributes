* text=auto

# Files that run inside Linux containers must keep LF endings even when
# checked out on Windows, otherwise the shebang/parser breaks at runtime.
*.sh text eol=lf
Dockerfile text eol=lf
docker-entrypoint.sh text eol=lf
